import { defineConfig } from 'astro/config';

// This configures Astro to build your site smoothly for GitHub Pages
export default defineConfig({
  site: 'https://github.io', // Change 'yourusername' to your real GitHub username
  base: '/portfolio',                     // Change 'portfolio' to your exact repository name
});
